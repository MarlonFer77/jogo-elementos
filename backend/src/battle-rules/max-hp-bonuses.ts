/** Mirrors MaxHpBonuses in max_hp_bonuses.dart — id -> HP bonus amount. No
 * name/description, same reasoning as elsewhere in this module. */
export const maxHpBonusesById: Readonly<Record<string, number>> = {
  vitality: 20,
};
